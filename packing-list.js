const packingList = ["bowls", "plates", "pots", "pans", "eating utensils", "cups", "cooking utensils"];

console.log("kitchen stuff to pack:");
let i = 0;
while (i < packingList.length) {
  console.log(packingList[i]);
  i++;
}